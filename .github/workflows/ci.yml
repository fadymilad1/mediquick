name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: MediQuick/package-lock.json

      - name: Install dependencies
        working-directory: MediQuick
        run: npm ci

      - name: Lint
        working-directory: MediQuick
        run: npm run lint

      - name: Test
        working-directory: MediQuick
        run: npm test
